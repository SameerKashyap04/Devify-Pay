{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @devify/database prisma generate && pnpm --filter @devify/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @devify/database prisma migrate deploy && node apps/api/dist/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
